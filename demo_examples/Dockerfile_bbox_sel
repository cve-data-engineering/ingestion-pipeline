# Using an older Alpine version (3.13) that likely contains older BusyBox packages
FROM alpine:3.13

# Update repositories
RUN apk update

# Install a BusyBox version known to be older and potentially vulnerable
# This is an example version; adjust if not found:
RUN apk update && apk upgrade

# The container now contains an older BusyBox that may be vulnerable.
